% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gof.R
\name{gof}
\alias{gof}
\title{Goodness-of-fit analysis for hawkesNet models.}
\usage{
gof(
  fit,
  net_obs,
  params_init,
  PMF_mark,
  cond_intensity,
  formula_RHS = NULL,
  time_window = c(0, 0.05),
  truncation = 100L,
  mark_decay = "activity",
  growth_only = FALSE,
  max_node_time = 1,
  inhom_bg = NULL,
  n_sim = 50L,
  cores = 7L,
  cores_outer = NULL,
  max_deg = 15L,
  k_esp = 15L,
  degree = 0L,
  esp = 0L,
  mu_multiplier = 5,
  seed_events = 0L,
  verbose = TRUE
)
}
\arguments{
\item{fit}{Fitted hawkesNet model object (from \code{fit_hawkesNet} or \code{fit_hawkesNet_inhom}).}

\item{net_obs}{Observed network (used for computing observed statistics).}

\item{params_init}{Initial parameter list used for fitting (needed to reconstruct full parameter structure).}

\item{PMF_mark}{Mark probability mass function (e.g., \code{PMF_mark_CS}).}

\item{cond_intensity}{Conditional intensity function (cached closure from fitting).
If \code{inhom_bg} is provided, \code{cond_intensity_inhom} will be used automatically for simulations.}

\item{formula_RHS}{Character string RHS of ERNM formula (e.g., "edges + triangles + star(c(2,3))").}

\item{time_window}{Time window for simulations (default c(0, 0.05)).}

\item{truncation}{Truncation parameter for mark PMF (default 100).}

\item{mark_decay}{Mark decay type: "node_entrance" or "activity" (default "activity").}

\item{growth_only}{Logical; if \code{TRUE}, edges only form when a node enters the network.
Default \code{FALSE}.}

\item{max_node_time}{Maximum node time (default 1).}

\item{inhom_bg}{Optional inhomogeneous background object from \code{prepare_inhomogeneous_background}.
If provided, simulations use \code{cond_intensity_inhom} with time-varying background rate to match the fitted model.}

\item{n_sim}{Number of simulated networks to generate (default 50).}

\item{cores}{Number of cores for parallelization (default 7). Ignored when \code{cores_outer} is set.}

\item{cores_outer}{Optional. When set, use PSOCK cluster with this many workers instead of fork.
Avoids BLAS/fork deadlocks on high-core machines (e.g. 100 cores). Capped at 60 for socket limits.}

\item{max_deg}{Maximum degree for degree distribution (default 15).}

\item{k_esp}{Maximum ESP count for ESP distribution (default 15).}

\item{degree}{Minimum degree to include in degree distribution (default 0).}

\item{esp}{Minimum ESP to include in ESP distribution (default 0).}

\item{mu_multiplier}{Multiplier for mu in simulations (default 5).}

\item{seed_events}{Optional integer. If \code{> 0}, the first \code{seed_events}
observed events are used to seed the simulation. The simulation then
generates subsequent events conditional on this initial history.
Recommended for sparse networks where cold-start simulation is difficult.}

\item{verbose}{Print progress messages (default TRUE).}
}
\value{
List with observed and simulated statistics and plots.
}
\description{
Simulates networks from a fitted model and compares observed vs simulated
network statistics (degree distribution, ESP, geodesic distances, waiting times).
}
\examples{
\donttest{
params <- list(mu = 0.5, beta_overall = 1, K = 0.3, beta_edges = 0.5, m = 1)
set.seed(1)
sim <- sim_hawkesNet(params, c(0, 3), PMF_mark_BA, cond_intensity,
                     verbose = FALSE, mu_multiplier = 5, truncation = 30)
fit <- fit_hawkesNet(params, c(0, 3), sim$net, PMF_mark_BA,
                     maxit = 10, verbose = FALSE, truncation = 30)
# Run GOF with 2 simulations for speed
g <- gof(fit, sim$net, params, PMF_mark_BA, cond_intensity,
         time_window = c(0, 3), n_sim = 2, truncation = 30)
names(g$plots)
}
}
