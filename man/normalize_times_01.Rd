% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{normalize_times_01}
\alias{normalize_times_01}
\title{Normalize node + edge "time" attributes to \verb{[0, 1]}}
\usage{
normalize_times_01(net, attr = "time", keep_na = TRUE, constant_value = 0)
}
\arguments{
\item{net}{A \code{network} object.}

\item{attr}{Name of the time attribute (default "time").}

\item{keep_na}{If TRUE, leave NA times as NA (default TRUE). If FALSE, error on NA.}

\item{constant_value}{Value to assign when all non-NA times are identical (default 0).}
}
\value{
The network with normalized times.
}
\description{
Uses get_times() to compute the global min/max across BOTH node and edge times,
then rescales vertex and edge "time" attributes into \verb{[0, 1]}.
}
\examples{
net <- network::network.initialize(2, directed = FALSE)
network::set.vertex.attribute(net, "time", c(10, 20))
net <- normalize_times_01(net)
network::get.vertex.attribute(net, "time")
}
