% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_hawkes.R
\name{simulate_hawkes_branching}
\alias{simulate_hawkes_branching}
\title{Simulate a univariate Hawkes process (branching structure)}
\usage{
simulate_hawkes_branching(mu, K, beta, T, seed = NULL)
}
\arguments{
\item{mu}{Numeric > 0. Background rate.}

\item{K}{Numeric >= 0. Mean number of children per event (branching ratio).
Typically K < 1 for a subcritical process.}

\item{beta}{Numeric > 0. Decay rate of the exponential triggering kernel.}

\item{T}{Numeric > 0. Maximum time horizon. Events are restricted to}

\item{seed}{Optional. Set an integer random seed for reproducibility. Default: \code{NULL}.}
}
\value{
A numeric vector of sorted event times within \verb{[0, T]}.
}
\description{
Generate event times from a Hawkes process with exponential triggering kernel
using a branching (cluster) representation:
\itemize{
\item \eqn{\mu} controls the background (immigrant) events.
\item \eqn{K} is the mean number of offspring per event.
\item \eqn{\beta} is the exponential decay rate for child arrival times after the parent.
}
}
\details{
\strong{Algorithm}:
\enumerate{
\item Draw background events (immigrants) from a Poisson(\eqn{\mu \times T}) process
and place them uniformly in \verb{[0, T]}.
\item For each event at time \eqn{t_p}, draw \eqn{N_p \sim \mathrm{Poisson}(K)} children.
Each child's time is \eqn{t_c = t_p + \Delta}, where \eqn{\Delta \sim \mathrm{Exp}(\beta)}.
Keep only those \eqn{t_c \le T}.
\item Each child then serves as a parent to further offspring, recursively, until no new events fall in \verb{[0,T]}.
}

This method gives the same distribution as a Hawkes process with intensity
\eqn{\lambda(t) = \mu + \sum_{t_i < t} K \beta e^{-\beta (t - t_i)}}, but may be faster
if you only need the final set of event times (particularly for subcritical K).
}
\examples{
# Simulate a Hawkes process with branching ratio 0.5
t <- simulate_hawkes_branching(mu = 1, K = 0.5, beta = 2, T = 10)
length(t)
\dontrun{
set.seed(123)
times <- simulate_hawkes_branching(mu = 0.2, K = 0.5, beta = 1, T = 10)
print(times)
}

}
