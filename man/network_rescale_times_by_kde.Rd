% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kde_background.R
\name{network_rescale_times_by_kde}
\alias{network_rescale_times_by_kde}
\title{Rescale vertex and edge times of a network using KDE baseline}
\usage{
network_rescale_times_by_kde(net, time_attr = "time", bw = NULL, grid_n = 4096)
}
\arguments{
\item{net}{A network object with vertex and edge time attributes}

\item{time_attr}{Name of the time attribute (default "time"). Must exist for vertices and edges.}

\item{bw}{Bandwidth for KDE; NULL = default}

\item{grid_n}{Grid size for KDE}
}
\value{
List with: net_rescaled (network with time_attr set to rescaled times),
time_window_rescaled = c(0, max_tau), mu_fit, Lambda_fun.
}
\description{
Gets all node and edge times from the network, estimates mu(t) via KDE,
computes Lambda(t), and replaces the time attribute with tau = Lambda(t).
}
\examples{
\donttest{
# Build a small network with time attributes
net <- network::network.initialize(5, directed = FALSE)
network::set.vertex.attribute(net, "time", c(0.1, 0.2, 0.5, 1.0, 2.0))
network::add.edge(net, 1, 2)
network::add.edge(net, 2, 3)
network::add.edge(net, 3, 4)
network::add.edge(net, 4, 5)
network::add.edge(net, 1, 5)
network::set.edge.attribute(net, "time", c(0.1, 0.3, 0.6, 1.2, 2.1))
res <- network_rescale_times_by_kde(net)
res$time_window_rescaled
}
}
