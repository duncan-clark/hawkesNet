% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hawkesNet.R
\name{cond_intensity}
\alias{cond_intensity}
\title{Conditional intensity for Hawkes network growth model}
\usage{
cond_intensity(
  new_net,
  t,
  mark_filtration = NULL,
  PMF_mark,
  params,
  new_edge_hash = NULL,
  times = NULL,
  ...
)
}
\arguments{
\item{new_net}{Current network (mark) state.}

\item{t}{Current time.}

\item{mark_filtration}{Observed network filtration (history).}

\item{PMF_mark}{Mark PMF function (e.g. \code{PMF_mark_BA} or \code{PMF_mark_CS}).}

\item{params}{List of parameters (\code{mu}, \code{beta_overall}, \code{K}, etc.).}

\item{new_edge_hash}{Optional hash of existing edges for fast lookup.}

\item{times}{Optional precomputed event times; if \code{NULL}, taken from \code{mark_filtration}.}

\item{...}{Arguments passed to \code{PMF_mark}.}
}
\value{
List with \code{result} (intensity value), \code{func} (function to evaluate intensity at new params), and optional debug fields.
}
\description{
Evaluates the conditional intensity at time \code{t} given the mark (network) and past events.
}
\examples{
\donttest{
params <- list(mu = 0.5, beta_overall = 1, K = 0.3, beta_edges = 0.5, m = 1)
set.seed(1)
sim <- sim_hawkesNet(params, c(0, 3), PMF_mark_BA, cond_intensity,
                     verbose = FALSE, mu_multiplier = 5, truncation = 30)
# Intensity at time 3.5 given the simulated network
intens <- cond_intensity(sim$net, 3.5, sim$net, PMF_mark_BA, params, truncation = 30)
intens$result
}
}
