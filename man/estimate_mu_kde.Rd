% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kde_background.R
\name{estimate_mu_kde}
\alias{estimate_mu_kde}
\title{Estimate inhomogeneous baseline rate mu(t) from event times using KDE}
\usage{
estimate_mu_kde(t, windowT = NULL, bw = NULL, grid_n = 4096)
}
\arguments{
\item{t}{Numeric vector of event times}

\item{windowT}{Length-2 vector (t0, t1) defining the observation window. If NULL, uses range(t).}

\item{bw}{Bandwidth for density(). If NULL, uses default (Silverman).}

\item{grid_n}{Number of grid points for the density / rate estimate}
}
\value{
List with: mu_fun (function mapping t -> rate), grid, mu_grid, bw, windowT.
mu_grid is the rate at each grid point (density * n so that integral ≈ n).
}
\description{
Estimate inhomogeneous baseline rate mu(t) from event times using KDE
}
\examples{
t <- sort(c(rexp(50, 2), runif(20, 0, 10)))
mu_fit <- estimate_mu_kde(t, windowT = c(0, 10))
mu_fit$mu_fun(5)
}
