% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hawkesNet.R
\name{merge_fit_params}
\alias{merge_fit_params}
\title{Merge fit$par with params_init to get full parameter list}
\usage{
merge_fit_params(par_vec, params_init, fixed_params = NULL)
}
\arguments{
\item{par_vec}{Named vector from \code{fit$fit$par}.}

\item{params_init}{Full initial parameter list (includes fixed params).}

\item{fixed_params}{Character vector of fixed param names (unused; for API consistency).}
}
\value{
List with full params (fitted + fixed).
}
\description{
When \code{fixed_params} is used, \code{fit$par} contains only free parameters.
This helper merges fitted values with \code{params_init} so fixed params (e.g. mu, K)
are included. Needed for \code{compensators_hawkesNet}, \code{ks_test_pval_hawkesNet}.
}
\examples{
\donttest{
params_init <- list(mu = 0.5, beta_overall = 1, K = 0.3, beta_edges = 0.5, m = 1)
par_vec <- c(mu = 0.6, beta_overall = 1.1)
# Merge fitted mu/beta_overall with initial K/beta_edges/m
pfull <- merge_fit_params(par_vec, params_init)
pfull$mu
pfull$K
}
}
