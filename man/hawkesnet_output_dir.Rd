% Generated by roxygen2: do not edit by hand
\name{hawkesnet_output_dir}
\alias{hawkesnet_output_dir}
\alias{hawkesnet_paper_figures_dir}
\title{Resolve the hawkesNet run-output directory}
\usage{
hawkesnet_output_dir(pkg_root = NULL, create = TRUE, subdirs = NULL)

hawkesnet_paper_figures_dir(pkg_root = NULL, create = TRUE)
}
\arguments{
\item{pkg_root}{Character path to the package root (directory with DESCRIPTION).
If \code{NULL}, uses \code{getwd()}.}

\item{create}{Logical; create the directory if missing (default \code{TRUE}).}

\item{subdirs}{Optional character vector of subdirectories to ensure exist
(e.g. \code{c("runs", "logs", "paper_figures", "diagnostics")}).}
}
\value{
Normalized path to the output directory.
}
\description{
Cluster studies and SLURM jobs should write RDS, logs, and paper figures
outside the package git checkout. Resolution order:
\enumerate{
\item Environment variable \code{HAWKESNET_OUTPUT_DIR} (recommended on NeSI)
\item An existing \code{cluster_output/} directory found by walking up from
\code{pkg_root} (typical laptop sync folder)
\item Sibling path \code{file.path(dirname(pkg_root), "cluster_output")}
}
}
