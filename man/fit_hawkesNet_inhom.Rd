% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hawkesNet.R
\name{fit_hawkesNet_inhom}
\alias{fit_hawkesNet_inhom}
\title{Fit HawkesGrowthNet with inhomogeneous (KDE) background}
\usage{
fit_hawkesNet_inhom(
  params_init,
  time_window,
  mark_filtration,
  PMF_mark,
  mu_vec,
  integral_bg,
  maxit,
  trace = 0,
  reltol = 1e-08,
  parscale = NULL,
  fixed_params = NULL,
  cache_intensity = TRUE,
  combine_intensity = TRUE,
  method = "Nelder-Mead",
  verbose = TRUE,
  parallel_type = "auto",
  ...
)
}
\arguments{
\item{params_init}{List of initial parameter values.}

\item{time_window}{Numeric \code{c(t0, t1)}.}

\item{mark_filtration}{Observed network.}

\item{PMF_mark}{Mark PMF function (e.g. \code{\link{PMF_mark_BA}} or \code{\link{PMF_mark_CS}}).}

\item{mu_vec}{Background rate at each event time (from
\code{\link{prepare_inhomogeneous_background}}).}

\item{integral_bg}{Integral of background over time window.}

\item{maxit}{Maximum number of iterations for the optimizer.}

\item{trace}{Trace level (default 0).}

\item{reltol}{Relative convergence tolerance (default 1e-8).}

\item{parscale}{Scale vector for parameters (default all 1).}

\item{fixed_params}{Character vector of parameter names to hold fixed.}

\item{cache_intensity}{If \code{TRUE}, cache intensity functions (default \code{TRUE}).}

\item{combine_intensity}{If \code{TRUE} (default) and fitting with an
inhomogeneous background (\code{mu_vec} supplied), combine per-event closures
into one vectorized closure for faster evaluation.}

\item{method}{Optimization method: \code{"Nelder-Mead"} (default, derivative-free) or
\code{"L-BFGS-B"} (gradient-based with box constraints).}

\item{verbose}{Print diagnostics to \code{stderr} (default \code{TRUE}).}

\item{parallel_type}{Parallelization strategy for intensity cache build:
\code{"auto"} (default), \code{"psock"}, or \code{"fork"}.}

\item{...}{Passed to \code{\link{fit_hawkesNet}} / \code{PMF_mark}
(e.g. \code{formula_RHS}, \code{truncation}, \code{cores}).}
}
\value{
Same as \code{\link{fit_hawkesNet}}.
}
\description{
Backward-compatible wrapper around \code{\link{fit_hawkesNet}}.
Equivalent to calling
\code{fit_hawkesNet(..., mu_vec = mu_vec, integral_bg = integral_bg)}.
}
\examples{
\donttest{
params <- list(mu = 2, beta_overall = 1, K = 0.3, beta_edges = 0.5, m = 1)
set.seed(1)
sim <- sim_hawkesNet(params, c(0, 10), PMF_mark_BA, cond_intensity,
                     verbose = FALSE, mu_multiplier = 5, truncation = 30)
# Prepare inhomogeneous background from the simulated data
inhom <- prepare_inhomogeneous_background(sim$net)
# Fit using the inhomogeneous background
fit <- fit_hawkesNet_inhom(params, c(0, 10), sim$net, PMF_mark_BA,
                           mu_vec = inhom$mu_vec, integral_bg = inhom$integral_bg,
                           maxit = 10, truncation = 30)
fit$fit_table
}
}
\seealso{
\code{\link{fit_hawkesNet}}, \code{\link{prepare_inhomogeneous_background}}
}
